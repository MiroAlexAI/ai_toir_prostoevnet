# 🚀 Быстрый Старт - Функция Перевода

## Шаг 1: Настройка API Ключа

### 1.1 Получите API ключ OpenRouter

1. Перейдите на [https://openrouter.ai/](https://openrouter.ai/)
2. Зарегистрируйтесь или войдите в аккаунт
3. Перейдите в раздел **Keys** в меню
4. Нажмите **Create Key**
5. Скопируйте созданный ключ

### 1.2 Настройте переменные окружения

Откройте файл `.env.local` в корне проекта и добавьте:

```env
OPENROUTER_API_KEY=sk-or-v1-ваш-ключ-здесь
```

**⚠️ ВАЖНО:** После изменения `.env.local` необходимо **перезапустить dev-сервер**!

```bash
# Остановите сервер (Ctrl+C)
# Затем запустите снова:
npm run dev
```

## Шаг 2: Использование функции перевода

### 2.1 Откройте приложение
Перейдите на [http://localhost:3000](http://localhost:3000)

### 2.2 Введите URL статьи
Вставьте ссылку на англоязычную статью, например:
- https://techcrunch.com/...
- https://medium.com/...
- https://dev.to/...

### 2.3 Нажмите кнопку "Перевод"
Зеленая кнопка справа в ряду кнопок

### 2.4 Дождитесь результата
- Появится индикатор "🔄 Выполняется перевод статьи..."
- После завершения увидите переведенный текст в блоке "Результат"

## Шаг 3: Проверка работы

### Проверьте логи сервера
В терминале должны появиться записи:
```
POST /api/parse 200 in XXXms
POST /api/translate 200 in XXXms  ← должен быть 200, не 500!
```

### Если видите ошибку 500
Это означает проблему с API ключом:

1. **Проверьте `.env.local`:**
   - Ключ должен начинаться с `sk-or-v1-`
   - Нет лишних пробелов
   - Файл находится в корне проекта

2. **Перезапустите сервер:**
   ```bash
   # Ctrl+C для остановки
   npm run dev
   ```

3. **Проверьте баланс на OpenRouter:**
   - Убедитесь, что на аккаунте есть кредиты
   - Бесплатная модель должна работать без оплаты

## Шаг 4: Тестовый пример

Попробуйте перевести короткую статью:
```
https://example.com/short-article
```

Ожидаемый результат:
```
🌐 Перевод статьи:

[Переведенный текст статьи на русском языке]
```

## Устранение проблем

### Ошибка: "OPENROUTER_API_KEY не настроен"
**Решение:** Добавьте ключ в `.env.local` и перезапустите сервер

### Ошибка: "Ошибка при обращении к OpenRouter API"
**Возможные причины:**
- Неверный API ключ
- Нет интернет-соединения
- Проблемы на стороне OpenRouter

**Решение:** 
1. Проверьте ключ на [openrouter.ai/keys](https://openrouter.ai/keys)
2. Убедитесь в наличии интернета
3. Проверьте статус OpenRouter

### Ошибка: "Ошибка при парсинге статьи"
**Возможные причины:**
- Неверный URL
- Сайт блокирует парсинг
- Сайт недоступен

**Решение:**
1. Проверьте корректность URL
2. Попробуйте другую статью
3. Убедитесь, что сайт доступен

## Дополнительная информация

### Используемая модель
- **Модель:** `zhipu-ai/glm-4-9b-chat:free`
- **Провайдер:** OpenRouter AI
- **Стоимость:** Бесплатная

### Ограничения
- Максимум ~4000 токенов на запрос
- Скорость зависит от загрузки API
- Качество перевода зависит от модели

### Поддержка
Если возникли проблемы, проверьте:
1. Логи в терминале dev-сервера
2. Консоль браузера (F12)
3. Файл `.env.local`

---

**Готово!** Теперь вы можете переводить статьи одним кликом! 🎉
